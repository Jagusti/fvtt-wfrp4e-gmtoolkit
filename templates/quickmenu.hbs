<div class="standard-form gmtoolkit quickmenu">
  {{#each quickMenu}}
    <button class="gmtoolkit quickmenu" data-action="{{action}}" data-name="{{name}}">{{#if icon}}<img src="{{icon}}" class="gmtoolkit quickmenu" />{{/if}}{{label}}</button>
  {{/each}}
</div>